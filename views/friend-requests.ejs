<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>طلبات الصداقة</title>
  <style>
    body {
      margin: 0;
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Helvetica, Arial, sans-serif;
      background-color: #f0f0f0;
      direction: rtl;
    }

    .container {
      max-width: 800px;
      margin: 0 auto;
      background: white;
      min-height: 100vh;
      box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
    }

    .header {
      background: linear-gradient(135deg, #075e54, #128c7e);
      color: white;
      padding: 25px 20px;
      text-align: center;
      position: relative;
    }

    .header h1 {
      margin: 0;
      font-size: 24px;
      font-weight: 600;
    }

    .requests-count {
      background: rgba(255, 255, 255, 0.2);
      padding: 4px 12px;
      border-radius: 15px;
      font-size: 14px;
      margin-top: 10px;
      display: inline-block;
    }

    .requests-list {
      list-style: none;
      padding: 0;
      margin: 0;
    }

    .request-item {
      display: flex;
      align-items: center;
      justify-content: space-between;
      padding: 20px;
      border-bottom: 1px solid #f0f0f0;
      transition: background-color 0.3s ease;
    }

    .request-item:hover {
      background-color: #f8f9fa;
    }

    .request-item:last-child {
      border-bottom: none;
    }

    .requester-info {
      display: flex;
      align-items: center;
      flex: 1;
    }

    .requester-avatar {
      width: 50px;
      height: 50px;
      border-radius: 50%;
      background: linear-gradient(135deg, #667eea, #764ba2);
      display: flex;
      align-items: center;
      justify-content: center;
      color: white;
      font-weight: bold;
      font-size: 18px;
      margin-left: 15px;
    }

    .requester-name {
      font-size: 16px;
      font-weight: 600;
      color: #2d3748;
    }

    .request-actions {
      display: flex;
      gap: 10px;
    }

    .accept-btn {
      background: linear-gradient(135deg, #25d366, #128c7e);
      color: white;
      border: none;
      padding: 10px 20px;
      border-radius: 20px;
      cursor: pointer;
      font-size: 14px;
      font-weight: 500;
      transition: all 0.3s ease;
      box-shadow: 0 2px 8px rgba(37, 211, 102, 0.3);
    }

    .accept-btn:hover {
      background: linear-gradient(135deg, #128c7e, #075e54);
      transform: translateY(-1px);
      box-shadow: 0 4px 12px rgba(37, 211, 102, 0.4);
    }

    .reject-btn {
      background: linear-gradient(135deg, #ff6b6b, #ee5a52);
      color: white;
      border: none;
      padding: 10px 20px;
      border-radius: 20px;
      cursor: pointer;
      font-size: 14px;
      font-weight: 500;
      transition: all 0.3s ease;
      box-shadow: 0 2px 8px rgba(255, 107, 107, 0.3);
      text-decoration: none;
      display: inline-block;
    }

    .reject-btn:hover {
      background: linear-gradient(135deg, #ee5a52, #d63031);
      transform: translateY(-1px);
      box-shadow: 0 4px 12px rgba(255, 107, 107, 0.4);
      color: white;
      text-decoration: none;
    }

    .navigation {
      display: flex;
      justify-content: center;
      gap: 15px;
      padding: 20px;
      background: #f8f9fa;
      border-top: 1px solid #e2e8f0;
      flex-wrap: wrap;
    }

    .nav-link {
      background: white;
      color: #075e54;
      padding: 12px 20px;
      border-radius: 25px;
      text-decoration: none;
      font-size: 14px;
      font-weight: 500;
      transition: all 0.3s ease;
      border: 2px solid #075e54;
      box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
    }

    .nav-link:hover {
      background: #075e54;
      color: white;
      transform: translateY(-2px);
      box-shadow: 0 4px 10px rgba(7, 94, 84, 0.3);
      text-decoration: none;
    }

    .empty-state {
      text-align: center;
      padding: 60px 20px;
      color: #718096;
    }

    .empty-state h3 {
      color: #4a5568;
      margin-bottom: 10px;
    }

    .empty-icon {
      font-size: 48px;
      margin-bottom: 20px;
      opacity: 0.5;
    }

    @media (max-width: 768px) {
      .container {
        margin: 0;
        box-shadow: none;
      }

      .request-item {
        padding: 15px;
        flex-direction: column;
        align-items: flex-start;
        gap: 15px;
      }

      .request-actions {
        width: 100%;
        justify-content: center;
      }

      .requester-avatar {
        width: 45px;
        height: 45px;
        font-size: 16px;
      }

      .navigation {
        padding: 15px;
        gap: 10px;
      }

      .nav-link {
        padding: 10px 15px;
        font-size: 13px;
      }
    }

    @keyframes fadeIn {
      from {
        opacity: 0;
        transform: translateY(10px);
      }
      to {
        opacity: 1;
        transform: translateY(0);
      }
    }

    .request-item {
      animation: fadeIn 0.4s ease-out;
    }

    .request-time {
      font-size: 12px;
      color: #718096;
      margin-top: 5px;
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="header">
      <h1>📩 طلبات الصداقة</h1>
      <div class="requests-count">عدد الطلبات: <%= requests.length %></div>
    </div>

    <% if (requests.length > 0) { %>
      <ul class="requests-list">
        <% requests.forEach(r => { %>
          <li class="request-item">
            <div class="requester-info">
              <div class="requester-avatar">
                <%= r.requester.username.charAt(0).toUpperCase() %>
              </div>
              <div>
                <div class="requester-name"><%= r.requester.username %></div>
                <div class="request-time">طلب مرسل</div>
              </div>
            </div>
            <div class="request-actions">
              <form action="/friend-accept/<%= r.requester._id %>" method="POST" style="display:inline">
                <button type="submit" class="accept-btn">✓ قبول</button>
              </form>
              <a href="/friend-reject/<%= r.requester._id %>" class="reject-btn">✗ رفض</a>
            </div>
          </li>
        <% }) %>
      </ul>
    <% } else { %>
      <div class="empty-state">
        <div class="empty-icon">📭</div>
        <h3>لا توجد طلبات صداقة</h3>
        <p>سيظهر هنا أي طلبات صداقة جديدة ترسل إليك</p>
      </div>
    <% } %>

    <div class="navigation">
      <a href="/" class="nav-link">👥 أصدقائي</a>
      <a href="/users" class="nav-link">🔍 المستخدمون</a>
      <a href="/chat" class="nav-link">💬 الشات العام</a>
      <a href="/logout" class="nav-link">🚪 تسجيل الخروج</a>
    </div>
  </div>
</body>
</html>
