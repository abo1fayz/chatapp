<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>المستخدمون</title>
  <style>
    body {
      margin: 0;
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Helvetica, Arial, sans-serif;
      background-color: #f0f0f0;
      direction: rtl;
    }

    .container {
      max-width: 800px;
      margin: 0 auto;
      background: white;
      min-height: 100vh;
      box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
    }

    .header {
      background: linear-gradient(135deg, #075e54, #128c7e);
      color: white;
      padding: 25px 20px;
      text-align: center;
      position: relative;
    }

    .header h1 {
      margin: 0;
      font-size: 24px;
      font-weight: 600;
    }

    .users-count {
      background: rgba(255, 255, 255, 0.2);
      padding: 4px 12px;
      border-radius: 15px;
      font-size: 14px;
      margin-top: 10px;
      display: inline-block;
    }

    .users-list {
      list-style: none;
      padding: 0;
      margin: 0;
    }

    .user-item {
      display: flex;
      align-items: center;
      justify-content: space-between;
      padding: 20px;
      border-bottom: 1px solid #f0f0f0;
      transition: background-color 0.3s ease;
    }

    .user-item:hover {
      background-color: #f8f9fa;
    }

    .user-item:last-child {
      border-bottom: none;
    }

    .user-info {
      display: flex;
      align-items: center;
      flex: 1;
    }

    .user-avatar {
      width: 50px;
      height: 50px;
      border-radius: 50%;
      background: linear-gradient(135deg, #667eea, #764ba2);
      display: flex;
      align-items: center;
      justify-content: center;
      color: white;
      font-weight: bold;
      font-size: 18px;
      margin-left: 15px;
    }

    .user-name {
      font-size: 16px;
      font-weight: 600;
      color: #2d3748;
    }

    .user-status {
      font-size: 12px;
      color: #718096;
      margin-top: 4px;
    }

    .request-btn, .cancel-btn, .friend-btn {
      padding: 10px 20px;
      border-radius: 20px;
      cursor: pointer;
      font-size: 14px;
      font-weight: 500;
      transition: all 0.3s ease;
      text-decoration: none;
      display: inline-block;
      border: none;
    }

    .request-btn {
      background: linear-gradient(135deg, #667eea, #764ba2);
      color: white;
      box-shadow: 0 2px 8px rgba(102, 126, 234, 0.3);
    }

    .request-btn:hover {
      background: linear-gradient(135deg, #5a6fd8, #6b46c1);
      transform: translateY(-1px);
      box-shadow: 0 4px 12px rgba(102, 126, 234, 0.4);
    }

    .cancel-btn {
      background: linear-gradient(135deg, #f6ad55, #ed8936);
      color: white;
      box-shadow: 0 2px 8px rgba(246, 173, 85, 0.3);
    }

    .cancel-btn:hover {
      background: linear-gradient(135deg, #ed8936, #dd6b20);
      transform: translateY(-1px);
      box-shadow: 0 4px 12px rgba(246, 173, 85, 0.4);
    }

    .friend-btn {
      background: linear-gradient(135deg, #48bb78, #38a169);
      color: white;
      box-shadow: 0 2px 8px rgba(72, 187, 120, 0.3);
    }

    .friend-btn:hover {
      background: linear-gradient(135deg, #38a169, #2f855a);
      transform: translateY(-1px);
      box-shadow: 0 4px 12px rgba(72, 187, 120, 0.4);
    }

    .pending-badge {
      background: #f6ad55;
      color: white;
      padding: 4px 12px;
      border-radius: 15px;
      font-size: 12px;
    }

    .navigation {
      display: flex;
      justify-content: center;
      gap: 15px;
      padding: 20px;
      background: #f8f9fa;
      border-top: 1px solid #e2e8f0;
      flex-wrap: wrap;
    }

    .nav-link {
      background: white;
      color: #075e54;
      padding: 12px 20px;
      border-radius: 25px;
      text-decoration: none;
      font-size: 14px;
      font-weight: 500;
      transition: all 0.3s ease;
      border: 2px solid #075e54;
      box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
    }

    .nav-link:hover {
      background: #075e54;
      color: white;
      transform: translateY(-2px);
      box-shadow: 0 4px 10px rgba(7, 94, 84, 0.3);
      text-decoration: none;
    }

    .empty-state {
      text-align: center;
      padding: 60px 20px;
      color: #718096;
    }

    .empty-state h3 {
      color: #4a5568;
      margin-bottom: 10px;
    }

    @media (max-width: 768px) {
      .container {
        margin: 0;
        box-shadow: none;
      }

      .user-item {
        padding: 15px;
        flex-direction: column;
        align-items: flex-start;
        gap: 15px;
      }

      .user-actions {
        width: 100%;
        text-align: center;
      }

      .user-avatar {
        width: 45px;
        height: 45px;
        font-size: 16px;
      }

      .navigation {
        padding: 15px;
        gap: 10px;
      }

      .nav-link {
        padding: 10px 15px;
        font-size: 13px;
      }
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="header">
      <h1>👥 المستخدمون</h1>
      <div class="users-count">عدد المستخدمين: <%= users.length %></div>
    </div>

    <% if (users.length > 0) { %>
      <ul class="users-list">
        <% users.forEach(user => { %>
          <li class="user-item">
            <div class="user-info">
              <div class="user-avatar">
                <%= user.username.charAt(0).toUpperCase() %>
              </div>
              <div>
                <div class="user-name"><%= user.username %></div>
                <div class="user-status">● متصل الآن</div>
              </div>
            </div>
            <div class="user-actions">
              <% if (friendIds.includes(user._id.toString())) { %>
                <span class="friend-btn">✓ صديق</span>
              <% } else if (sentIds.includes(user._id.toString())) { %>
                <form action="/friend-cancel/<%= user._id %>" method="POST" style="display:inline">
                  <button type="submit" class="cancel-btn">✗ إلغاء الطلب</button>
                </form>
              <% } else if (receivedIds.includes(user._id.toString())) { %>
                <span class="pending-badge">طلب ورد</span>
              <% } else { %>
                <form action="/friend-request/<%= user._id %>" method="POST" style="display:inline">
                  <button type="submit" class="request-btn">+ إرسال طلب</button>
                </form>
              <% } %>
            </div>
          </li>
        <% }) %>
      </ul>
    <% } else { %>
      <div class="empty-state">
        <h3>لا يوجد مستخدمون آخرون</h3>
        <p>سيظهر هنا المستخدمون المسجلون في التطبيق</p>
      </div>
    <% } %>

    <div class="navigation">
      <a href="/friends" class="nav-link">👥 أصدقائي</a>
      <a href="/friend-requests" class="nav-link">📩 طلبات الصداقة</a>
      <a href="/chat" class="nav-link">💬 الشات العام</a>
      <a href="/logout" class="nav-link">🚪 تسجيل الخروج</a>
    </div>
  </div>
</body>
</html>